#include "screenshot.h"

namespace cps {
bool GetScreenshotImageByteData(unsigned char **image_bytes,
                                size_t *width,
                                size_t *height,
                                size_t *bytes_len,
                                bool compress) {
  // todo
}
}